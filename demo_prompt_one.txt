You are a network diagnostics model. Use ONLY the JSON input.

Task:
1) Re-compute and return these transformed metrics from telemetry summary:
   - cpu_avg_pct
   - ram_avg_pct
   - tcp_established_avg
   - tcp_time_wait_avg
   - disk_write_avg
2) Detect data contradiction between HTML system RAM and telemetry RAM average.
3) Pick the better source for incident reasoning and justify.
4) Infer likely root cause for the issue.
5) Provide evidence-backed diagnostics and fixes.

Return STRICT JSON (no markdown):
{
  "case_id": "string",
  "computed_metrics": {
    "cpu_avg_pct": number,
    "ram_avg_pct": number,
    "tcp_established_avg": number,
    "tcp_time_wait_avg": number,
    "disk_write_avg": number
  },
  "contradictions_detected": [
    {
      "field": "string",
      "source_a": "string",
      "source_b": "string",
      "explanation": "string"
    }
  ],
  "chosen_source_for_ram": {
    "source": "html|telemetry|hybrid",
    "justification": "string"
  },
  "root_cause": {
    "summary": "string",
    "confidence": "low|medium|high"
  },
  "evidence_points": ["string"],
  "diagnostics": ["string"],
  "fixes": ["string"]
}

Rules:
- Do not invent data not present.
- If uncertain, say so explicitly.
- Tie every root-cause claim to evidence_points.
